<table>
	<% @productsprices.find_each do |productsprice| %>
		<tr>
			<td><% productsprice.currency.description %></td>
			<td><% productsprice.price %></td>
			<td><%= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		</tr>
	<% end %>
</table>